import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(theme => ({
	root: {}
}));

function Apps() {
	 return (
		<div className="w-full">
			<h2>Apps</h2>
		</div>
	);
}

export default Apps;
